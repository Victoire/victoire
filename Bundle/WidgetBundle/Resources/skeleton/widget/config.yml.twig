victoire_core:
    widgets:
        {{ widget|lower }}:
            class: "Victoire\\Widget\\{{ widget }}Bundle\\Entity\\Widget{{ widget }}"
            name: {{ widget|lower }}
            parent: {{parent|lower}}
