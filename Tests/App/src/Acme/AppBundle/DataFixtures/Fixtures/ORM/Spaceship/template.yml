Victoire\Bundle\I18nBundle\Entity\ViewTranslation:
    template_spaceship_fr:
        name: "Vaisseau spatial {{item.name}}"
        slug: "vaisseau-spatial-{{item.slug}}"
        url: "vaisseau-spatial-{{item.slug}}"
        locale: fr
    template_spaceship_en:
        name: "Spaceship {{item.name}}"
        slug: "spaceship-{{item.slug}}"
        url: "spaceship-{{item.slug}}"
        locale: en

Victoire\Bundle\BusinessPageBundle\Entity\BusinessTemplate:
    template_spaceship:
        backendName: "Spaceship template"
        businessEntityId: spaceship
        author: @anakin
        template: @template_base
        status: published
        publishedAt: <dateTime('now')>
        createdAt: <dateTime('now')>
        updatedAt: <dateTime('now')>
        parent: @home
        translations:
            - @template_spaceship_fr
            - @template_spaceship_en
