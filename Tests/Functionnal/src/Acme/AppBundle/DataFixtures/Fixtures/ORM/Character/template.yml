Victoire\Bundle\BusinessPageBundle\Entity\BusinessTemplate:
    template_character:
        name: "Character {{item.name}}"
        backendName: "Character template"
        slug: "character-{{item.slug}}"
        url: "character-{{item.slug}}"
        businessEntityId: character
        author: @anakin
        locale: fr
        template: @template_base
        status: published
        publishedAt: <dateTime('now')>
        createdAt: <dateTime('now')>
        updatedAt: <dateTime('now')>

    template_jedi:
        name: "Jedi {{item.name}}"
        slug: "jedi-{{item.slug}}"
        url: "jedi-{{item.slug}}"
        businessEntityId: jedi
        author: @anakin
        locale: fr
        template: @template_base
        status: published
        publishedAt: <dateTime('now')>
        createdAt: <dateTime('now')>
        updatedAt: <dateTime('now')>

    template_mercenary:
        name: "Mercenary {{item.name}}"
        slug: "mercenary-{{item.slug}}"
        url: "mercenary-{{item.slug}}"
        businessEntityId: character
        query: WHERE itme.type = 'mercenary'
        author: @anakin
        locale: fr
        template: @template_base
        status: published
        publishedAt: <dateTime('now')>
        createdAt: <dateTime('now')>
        updatedAt: <dateTime('now')>
